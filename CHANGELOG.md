# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：`主版本.次版本.修订号[-预发布标识]`。

- 正式版**不带**任何预发布后缀；预发布只允许 `-alpha.N` / `-beta.N` / `-rc.N`。
- 主版本：布局或数据格式的破坏性变更（对套壳应用而言，通常是"页面架构整体换掉"）。
- 次版本：新增用户可见能力，向后兼容。
- 修订号：缺陷修复与样式微调。

## [Unreleased]

### Added

- release 签名支持：仓库根放 `keystore.properties` 即自动签名；未配置时产物文件名
  标成 `-release-unsigned`，避免拿一个装不上的包去装。

### Changed

- 不再固定 `buildToolsVersion`，改由 AGP 选择，减少别人clone 后构建失败的情况。
- 移除 GitHub Actions 工作流。产物匿名下载本来就要 401 登录，它当不了分发渠道；
  发布改为本地签名后手动上传 Release。

## 1.0.1-alpha.1 — 2026-09-26

首次公开。这是预发布版本：只在 Android 模拟器的手机与平板两种配置上验证过，
没有自动化测试，真机覆盖有限。

### Added

- m.weibo.cn 的 WebView 容器：登录不外跳（拦截 `sinaweibo://` 等自定义协议）、
  返回键走网页历史、全屏视频交给原生容器、错误页与重试。
- 深浅色跟随系统：算法加深 + 图片/玻璃态补偿（头像反相规避、logo 保护、头像不垫白底）。
- 平板与横屏自适应：信息流双列、正文页限宽居中、悬浮导航改竖排。
- 悬浮导航与发博球，下滑自动收起；无返回入口的老架构页面用悬浮返回球兜底。
- 首页关注分组选择框；私信会话页与分组弹层让出下拉刷新手势。
- 信息流广告拦截（百度联盟槽位的三层请求）。
- 长按图片下载 / 复制图片地址（走系统 `DownloadManager`，存到 `Pictures/pulse/`）。
- 视频滑动快进快退。
- 会话失效时按 `/api/config` 纠正登录态，避免停在登录页。
- 构建产物按 `pulse-<版本>-<变体>.apk` 复制到仓库外的产物目录。

### Known issues

- 平板双列在图片加载完成时仍有单列内部重排造成的轻微跳动（实测 0.28–1.04 次/万像素）。
- 深色顶栏切换瞬间的闪烁：已按"预热 `backdrop-filter`"改动，但缺少可复现证据，未验证。
- 注入规则依赖站点类名，微博改版会导致部分页面样式回退。

[Unreleased]: https://github.com/Noyllopa/pulse/compare/v1.0.1-alpha.1...HEAD
