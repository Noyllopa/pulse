# 安全策略

## 报告一个安全问题

请**不要**开公开 issue。优先通过 GitHub 的
`Security` → `Report a vulnerability`（私有漏洞报告）提交；
如果该入口不可用，开一个标题只写"安全问题"的 issue，正文先不要写可利用细节，
等我回复后转私聊。

我会在 7 天内回复。这个项目是单人业余维护，请理解修复可能需要更久。

## 什么算安全问题

- 登录凭据（微博 Cookie）被泄露、被上传、被非预期进程读到
- WebView 配置导致任意页面能在应用上下文里执行代码
- 注入脚本被恶意微博内容利用（例如某条微博的正文/昵称能让脚本跳出沙箱做别的事）
- 明文流量放行范围被扩大到微博系以外的域名
- 备份、日志、下载路径把敏感数据写到公共位置

## 不在范围内

- 微博服务端自身的漏洞、账号被盗、站点内容问题
- 需要物理接触已 root 设备才能做的攻击
- 用户主动安装的 debug 构建被 DevTools 挂载（debug 构建按设计开放）

## 已知的安全相关设计决定

| 决定 | 原因 | 代价 |
| --- | --- | --- |
| DevTools 仅在 debug 构建开放 | release 包不应让任何人用 `chrome://inspect` 读到会话 | 排查线上问题需要单独装 debug 包 |
| 明文 http 只对微博系域名放行 | 微博登录/SSO 跳转链里含明文环节，全禁会卡死登录 | 这些域名下的明文请求可被同网段嗅探 |
| 允许第三方 Cookie | 微博跨域 SSO 需要 | 扩大跟踪面，仅限微博系域 |
| `allowBackup="true"` | 沿用模板默认值，尚未评估 | 系统备份可能包含登录态。**在意请关闭设备上的应用备份** |
| 不申请任何危险权限 | 只有 `INTERNET` 与 `≤28` 的写公共目录（下载图片用） | — |

## 版本支持

预发布阶段只支持最新版本的问题修复，不会回移到旧标签。
正式发布后会在本文件列出受支持的版本范围。
